# MODULE QA BOARD — POLYMIND CHINESE

> Bảng điều phối QA giữa **Claude (QA/Verify)** và **Codex (Fix)**. Nguồn sự thật về trạng thái QA từng module.
> Đọc cùng [`WORKLOG.md`](../../WORKLOG.md). **Không dựa vào trí nhớ phiên trước** — mọi trạng thái nằm ở đây.

- **Hệ thống:** POLYMIND CHINESE — quản lý học viên tiếng Trung (Next.js App Router + TypeScript strict + Supabase Auth/Postgres/Storage + Vercel).
- **Kiến trúc:** `src/features/*/server` (nghiệp vụ) · `src/lib/domain` (business rule thuần, có unit test) · RLS ở DB là **chốt chặn cuối**.
- **Test:** Vitest (unit/component) · pgTAP (constraint + RLS/IDOR) · Playwright (E2E 3 role).

---

## Chú thích trạng thái

- **QA Status:** `Pending` · `Analyzing` · `Test Cases Ready` · `Automated Tests Ready` · `Bugs Found` · `No Confirmed Bugs` · `Completed` · `Blocked`
- **Fix Status:** `Not Required` · `Waiting for Codex` · `Investigating` · `Fixed` · `Cannot Reproduce` · `Blocked` · `Needs Requirement Clarification`
- **Verification:** `Not Started` · `Waiting for Fix` · `Verifying` · `Verified` · `Partially Verified` · `Failed` · `Blocked`

> **Luật vàng:** người **fix** không được tự ghi `Verified`. Phải để agent kia **xác minh độc lập** — đọc diff, chạy test, dựng lại kịch bản lỗi. Đó là toàn bộ giá trị của mô hình này.

---

## Bảng module (sắp theo dependency + rủi ro)

|   # | ID  | Module                        | Phạm vi                                                                                            | Phase           | Rủi ro       | QA                    | Fix          | Verification |
| --: | --- | ----------------------------- | -------------------------------------------------------------------------------------------------- | --------------- | ------------ | --------------------- | ------------ | ------------ |
|   1 | M01 | Authentication & Session      | Login, logout, forgot/reset password, accept-invite, middleware, session cookie SSR                | P1              | **Critical** | Pending               | Not Required | Not Started  |
|   2 | M02 | Authorization & RLS           | `app.*` helper, RLS policy toàn bộ bảng, fail-closed, IDOR                                         | P2              | **Critical** | Pending               | Not Required | Not Started  |
|   3 | M03 | User & Account Management     | Invite (service role, server-only), khóa/mở tài khoản, đổi role, `profiles`                        | P1, P3          | **High**     | Pending               | Not Required | Not Started  |
|   4 | M04 | Students                      | CRUD hồ sơ, guardian (field, **không phải role**), link tài khoản                                  | P3              | High         | Pending               | Not Required | Not Started  |
|   5 | M05 | Teachers                      | CRUD hồ sơ, `teacher_code`, phân công lớp                                                          | P3              | Medium       | Pending               | Not Required | Not Started  |
|   6 | M06 | Courses & Curriculum          | Level, Course, Module, Lesson, Materials                                                           | P3              | Medium       | **Bugs Found**        | **Fixed**    | Not Started  |
|   7 | M07 | Classes                       | Lớp triển khai, sĩ số, hình thức, địa điểm, một GV phụ trách                                       | P3, P7          | High         | Pending               | Not Required | Not Started  |
|   8 | M08 | Schedules & Sessions          | Lịch lặp, **sinh buổi idempotent**, đổi lịch/học bù, lớp linh hoạt                                 | P3, P4          | **High**     | **Bugs Found**        | **Fixed**    | Not Started  |
|   9 | M09 | Enrollments                   | Ghi danh (**capacity race**), tạm dừng, **chuyển lớp giữ lịch sử**, rút, hoàn thành                | P3              | **High**     | Pending               | Not Required | Not Started  |
|  10 | M10 | Attendance                    | Bulk upsert (**không sinh trùng**), class-match trigger, attribution actor thật                    | P4              | **High**     | Pending               | Not Required | Not Started  |
|  11 | M11 | Assignments & Submissions     | Draft/publish, hạn nộp, nộp text/file, chấm điểm, **HV không sửa được score**                      | P4, P5          | **High**     | **Bugs Found**        | **Fixed**    | Not Started  |
|  12 | M12 | Assessments & Evaluations     | Điểm 0–100 + 6 kỹ năng, **draft ≠ publish**, xếp loại từ grading scale, `student_notes` staff_only | P4              | **High**     | **Bugs Found**        | **Fixed**    | Not Started  |
|  13 | M13 | Progress & Completion         | View tiến độ (tính, không nhập tay), readiness, xác nhận hoàn thành có audit                       | P4, P5          | Medium       | Pending               | Not Required | Not Started  |
|  14 | M14 | Tuition                       | Invoice/payment/**receipt duy nhất**, số dư = tính ra, **teacher DENY toàn bộ**                    | P6              | **High**     | Pending               | Not Required | Not Started  |
|  15 | M15 | Notifications & Announcements | In-app một chiều, **dedupe cron**, link có authorization                                           | P6              | Medium       | Pending               | Not Required | Not Started  |
|  16 | M16 | Reports & Export              | Báo cáo theo scope, **export giữ đúng filter đang chọn**                                           | P6              | Medium       | Pending               | Not Required | Not Started  |
|  17 | M17 | Dashboards                    | 3 dashboard theo role, KPI từ view                                                                 | P3–P5           | Low          | Pending               | Not Required | Not Started  |
|  18 | M18 | Storage & Files               | 6 bucket nghiệp vụ private, **object_path do server sinh**, signed URL ngắn hạn, IDOR file         | P2, P4, P5, P14 | **High**     | **Bugs Found**        | **Fixed**    | Not Started  |
|  19 | M19 | Audit Log                     | Append-only, **chỉ super admin đọc**, actor thật                                                   | P6              | Medium       | Pending               | Not Required | Not Started  |
|  20 | M20 | Security & Deployment         | Headers, rate limit, upload abuse, env/secret, production seed, RLS trên cloud                     | P7              | **High**     | Pending               | Not Required | Not Started  |
|  21 | M21 | Student Class Portal          | Lớp của tôi chỉ đọc; không roster; gộp lịch/tài liệu/chuyên cần/assessment/progress                | P14             | Medium       | Automated Tests Ready | Not Required | Not Started  |
|  22 | M22 | Flashcards & Wrong Review     | Signed media; publish scope; IDOR queue; answer key không xuống client; mastery atomic             | P14             | **High**     | Automated Tests Ready | Not Required | Not Started  |
|  22b| M24/M06 | Flashcard cấu trúc (Phase 16) | Thẻ từ vựng bản ghi có cấu trúc (§7ter); `media_paths`+GIN cho ảnh câu ví dụ; nhập hàng loạt idempotent DB; ★ thẻ khó; xáo trộn/phát tự động | P16 | **High** | **Verified (local + cloud schema)** | **`BUG-P16-001` Verified độc lập — Claude 2026-07-24 (Codex fix). ✅ Cloud 070–072 pushed + remote up-to-date (BLK-4 gỡ)** | Not Started |

**Trạng thái:** chưa bắt đầu đợt QA module toàn diện. Bug phát hiện trong lúc build được ghi ngay vào Verification Queue để không mất dấu.

---

## Handoff Queue (bug đang chờ fix)

| #   | Bug ID            | Module | Severity   | Khu vực nghi ngờ                                                                                                                                                                                                                                                                                                                                                                                             | Trạng thái                                                                                                                        |
| --- | ----------------- | ------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `UX-UIUX-M00-018` | M00    | **Medium** | `src/components/ui/button.tsx:37` — `icon: "size-10"` (40px) và media query `pointer: coarse` chỉ đặt `min-height: 44px`, **thiếu `min-width`**. Nút `Mở menu điều hướng` đo được **40×44**, chuẩn WCAG cần ≥44×44. `tests/e2e/accessibility-responsive.spec.ts` **đang đỏ trên cả `chromium` lẫn `mobile`** vì lỗi này | ⏸ **Có TRƯỚC đợt logo** — đã `git stash` toàn bộ thay đổi logo rồi chạy lại spec: **đỏ y hệt**. Đụng tầng dùng chung → chờ user duyệt (tiền lệ `DS-030`) |
| 2   | `UX-UIUX-M00-025` | M00    | **High**   | 🔴 **Server Component truyền React element vào prop `trigger`, Client Component đặt nó vào Radix `asChild` → `Children.only()` có lúc ném `Primitive.button failed to slot onto its children` → cả trang rơi vào error boundary `(dashboard)/error.tsx`.** Đo được **47/120 lượt hỏng (39%) · 99 lỗi "failed to slot"** ở `/admin/classes/[id]` trên Pixel 7. **Sống sót qua BA đợt QA vì trang lỗi có đúng một `<h1>` và không `<h2>` nào** — trùng khít triệu chứng "màn thiếu heading cấp 2": đợt 9 ghi thành lỗi a11y của sản phẩm, đợt 10 ghi thành "RSC còn stream", đầu đợt 11 tôi ghi thành "truy vấn máy chủ `throw`" — **cả ba đều sai**. Bác bỏ vế cuối bằng `webServer.stdout: "pipe"`: log `next dev` cho thấy **mọi request đều `200`**, lỗi nằm ở trình duyệt lúc render (bắt bằng `page.on("console")`) | ☑ **Fixed (Claude) — chờ xác minh độc lập.** Nút mở dialog nay do chính Client Component dựng (suy từ `isEdit`/`version`), không nhận element qua ranh giới RSC. 3 call site: `admin/classes/[id]`, `admin/courses/[id]`, `question-bank-page.tsx` (2 chỗ). **Sau sửa: `0/120` và `0/90`, giao diện không đổi.** Khoá **hai lớp**: (a) bỏ hẳn prop `trigger` khỏi ba component đó — cả sáu chỗ gọi đều là Server Component nên prop này vừa chết vừa chỉ có thể dùng sai, bỏ đi thì lỗi **không tái tạo được**; (b) bài kiểm **tĩnh** `tests/unit/rsc-aschild-trigger.test.ts` chặn cả lớp lỗi cho mọi prop tương lai (không dùng E2E: lỗi 47/120 lượt thì bài E2E chỉ là bài chập chờn — `DS-038`), **đã kiểm ngược**. Xác nhận lại sau khi bỏ prop: **0/24 · 0 lỗi** ngay cả lúc máy kiệt RAM |

---

## Verification Queue (fix đang chờ xác minh độc lập)

| Bug ID              | Module        | Fix của Codex                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Trạng thái                                                                                 |
| ------------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `BUG-P16-001` | M22/M06 | (Codex fix trong lượt xác minh Phase 16) `seed.dev.sql` và E2E từng ghim UUID sinh động của Course `VCB-BANK`, nên `db:reset → db:seed:dev` đỏ FK và bằng chứng E2E chỉ tái lập trên DB cũ. Sửa bằng khóa nghiệp vụ `courses.code`, thêm static regression test. Sau sửa: reset→seed exit 0, UTF-8 đúng; `flashcard-responsive` Chromium `16/16` + Pixel 7 `16/16`. Bug report: `modules/M22-flashcards/06-bug-report.md`. | ⏳ Chờ Claude/agent khác xác minh độc lập — **Codex là người sửa, không tự ghi Verified** |
| UX-UIUX-M02-001 · UX-UIUX-M00-021…024 · UX-UIUX-M05-002 · UX-UIUX-M04-003 · UX-UIUX-M01-004 · UX-UIUX-M09-005…007 | M11 | (Claude fix, `P18-T2`…`P18-T14` — khu **Quản trị M01–M12**, **13 bề mặt × 6 bề rộng × 2 project**) 🔴 **`UX-UIUX-M02-001` — 6 bảng GIẤU MẤT DỮ LIỆU trên điện thoại**: bản `md:hidden` bỏ hẳn **email + người giám hộ** (Học viên), **điện thoại + chuyên môn** (Giáo viên), **học phí + số buổi** (Khóa học), **khai giảng** (Lớp), **mã + điện thoại** (Tài khoản) — quản trị viên dùng điện thoại **không có cách nào** đọc được; **không spec nào báo đỏ** vì mỗi bản đều 'đúng' theo tiêu chí của chính nó (mẫu `UX-UIUX-M25-010`), và đây cũng là nguyên nhân `admin-accounts.smoke` đỏ ở `P17-T5`. 🔴 **`UX-UIUX-M00-021` — tương phản tab 4.07:1** ở **tầng dùng chung** `ui/tabs.tsx` (`text-foreground/60`, mức `serious`); khu học viên không dính vì tự đè màu — **đã quét axe `/student/class` + `/student/results` TRƯỚC khi sửa để chứng minh** (0 vi phạm). 🔴 tràn ngang **127px** `/admin`, **193px** `classes/[id]`, **106px** `courses/[id]`, **93px** `reports`; ⚠️ **chẩn đoán đầu SAI** — rải `min-w-0` theo mẫu `DS-039` xong đo lại **vẫn tràn nguyên**, nguyên nhân thật là `div.flex.shrink-0.flex-wrap` rộng **516px trong khung 360px** (`shrink-0` + `flex-wrap` triệt tiêu nhau) và dải 4 tab rộng **450px**. 🔴 **`UX-UIUX-M09-005` — `/admin/reports` in THẲNG chuỗi ISO `2026-07-15`** ra màn hình, trái `D-12`; không ai thấy vì **seed không có hóa đơn nào**. 🔴 `scrollable-region-focusable` mức `serious`. 🔸 **`<h2>` = 0 trên 12/13 màn** + nhảy cấp `h1→h3`; ~200 chỗ chữ <14px; **không `<caption>` nào**, `0/4` `th` có `scope`; `<select>` viền **1.27:1** (bản chép thứ 7–8). ⚠️ **Ba lỗi tự gây, bắt trước khi giao**: `<dl>` sai cấu trúc (axe `serious`, 10 node); **ô định danh dính không đổi màu theo hàng — E2E 14/14 xanh vẫn không bắt được, chỉ lộ khi nhìn ảnh chụp**; chặng Tab vô nghĩa. ⚠️ **Ba bẫy đo của chính mình**: contrast tự tính **18.52:1** vs axe **4.07:1** (`getComputedStyle` trả màu **chưa trộn nền**), probe chỉ báo ruột SVG, và 'tràn' của con nằm **trong** vùng cuộn. Vitest **226/226** (+6, kiểm ngược bằng cách cài đặt sai → đỏ đúng như mong đợi); `admin-responsive` **14/14**; spec liên quan **14/14**. Không migration, không đổi query/action/RPC/RLS/route/phân quyền/validation/nhãn. | ⏳ Chờ Codex/user xác minh độc lập — **Claude là người sửa, không tự ghi Verified** |
| UX-UIUX-M13-010 · UX-UIUX-M14-020…022 · UX-UIUX-M00-019 · UX-UIUX-M18-008 | M11 | (Claude fix, `P17-T5` — quality gate liên module M13→M19, **13 bề mặt × 6 bề rộng**) 🔴 **Hai lỗi tràn ngang là MỘT nguyên nhân gốc:** grid item mặc định `min-width: auto` nên không co được dưới min-content — `/teacher` tràn **31px**@360, `/teacher/classes` tràn **70px**@360 và **40px**@390 (thẻ lớp đo được **414px** ở cả hai viewport, tức bị min-content ghim). Kiểm chứng **trong trình duyệt trước khi sửa**: đặt `min-width: 0` → tràn về **0px** cả hai màn. 🔴 dải **8 tab** `/teacher/classes/[id]` cuộn ngang mà `tabindex=null` và chỉ **1** phần tử Tab tới được bên trong — Radix dùng **roving tabindex** nên "bên trong có `<Link>`" không cứu được; đúng lỗi `UX-UIUX-M21-009` lặp lại ở phía giáo viên. 🔸 tên lớp bị `truncate` mất hậu tố phân biệt `(Lớp 02)`/`(Ban Giám đốc)` — **E2E 14/14 xanh vẫn không bắt được**, chỉ lộ khi nhìn ảnh chụp (bài học `UX-UIUX-M19-007`); đã khoá bằng assertion đo bề rộng thật. 🔴 **`UX-UIUX-M00-019` — 9 spec E2E ghim UUID hàng seed**: `seed.sql` insert `classes`/`students`/`enrollments` không chỉ định `id` → UUID sinh mới sau **mỗi `db reset`**, nên **mọi con số "E2E xanh" của các phiên trước chỉ tái lập được trên DB chưa reset** (cùng loại `UX-UIUX-M18-007`); sửa bằng cách tra theo khóa nghiệp vụ ổn định. ✅ `UX-UIUX-M18-008` đóng: quét lại toàn `src/` ra **32 `SelectTrigger`, 0 chỗ vô danh**. ⚠️ **Hai lần suýt báo động giả, cả hai là lỗi ĐO**: `h1=0` do đếm khi RSC còn stream, contrast 4.45:1 do quét axe giữa lúc dialog fade-in — nếu tin lượt đo đầu thì đã sửa nhầm token dùng chung toàn app. Không migration, không đổi query/action/RPC/RLS/route/phân quyền. | ⏳ Chờ Codex/user xác minh độc lập — **Claude là người sửa, không tự ghi Verified** |
| UX-UIUX-M18-001…007 · UX-UIUX-M19-001…007 | M11 | (Claude fix, `P17-T3` + `P17-T4`) **M18 Đánh giá & Ghi chú** — 🔴 `ClassPicker` là **combobox KHÔNG CÓ TÊN** (axe `select-name`, critical): `SelectTrigger` render `role="combobox"`, mà role này **không lấy tên từ nội dung** nên chữ hiển thị bên trong không thành accessible name; quét repo ra 32 `SelectTrigger`/**6 vô danh**, `ClassPicker` dùng ở 3 trang gồm cả M19 → sửa một chỗ sạch hai module. 🔴 `FieldError` thiếu `role="alert"`/`id`, không ô nhập nào có `aria-invalid`/`aria-describedby`. 🔴 mỗi bản đánh giá không có heading + cụm nút icon **trùng tên nhau**, kể cả nút Gửi không thu hồi được. 🔴 **dialog nói SAI hậu quả** — sửa bản đã gửi thì học viên thấy ngay nhưng UI ghi "Lưu form không gửi cho học viên"; **chỉ sửa câu chữ, không đổi luật nghiệp vụ**. 🔴 `evaluation.smoke` **hỏng sẵn từ trước** (dùng `page.once("dialog")` cho AlertDialog của Radix) nên nhánh Gửi **chưa từng chạy** — kiểm chứng bằng `git stash` là lỗi có sẵn. **M19 Báo cáo lớp** — cả trang chỉ có **một heading**; bảng 7 cột **vừa không cuộn được bằng bàn phím vừa không cuộn** (thiếu `tabIndex` + thiếu `min-width`, bị bóp còn ~51px/cột @360); thiếu `<caption>`/`scope`; số liệu can thiệp ở **12px**; thêm biểu đồ `AttendanceBars` (**không** thêm query/công thức/ngưỡng, **không** dùng `recharts`); nhãn "Cần chú ý" **bị cắt cụt** — lỗi tự gây, E2E 8/8 xanh vẫn không bắt được, chỉ lộ khi nhìn ảnh chụp. **Export/date-range của M19 ghi N/A có bằng chứng** (`DS-037`). E2E **16/16 + 16/16**, lượt chạy chung **38/38**, `evaluation.smoke` 2/2, `report.smoke` 2/2, a11y 2/2; lint/typecheck/build xanh. ⚠️ **Vitest 218–220/220 — chưa ổn định**, timeout 5s ở file không liên quan, chạy riêng đều xanh (`UX-UIUX-M19-008`). Không migration. | ⏳ Chờ Codex/user xác minh độc lập — **Claude là người sửa, không tự ghi Verified** |
| UX-UIUX-M17-001…007 + UX-UIUX-M00-018 | M11           | (Claude fix, `P17-T2`) Kiểm tra/Thi giáo viên: **giờ đóng thi lệch múi giờ** — `exam-dashboard.tsx:245`, đo Node cùng một `closes_at`: Berlin **14/8 19:30** vs VN **15/8 00:30**, lệch hẳn một ngày; **hai landmark `<main>` lồng nhau** ở `grading-workspace.tsx` (dùng chung, hỏng cả màn chấm Bài tập lẫn Thi — E2E đếm được 2); học viên đang chọn **chỉ báo bằng màu** → thêm `aria-current` + `role="img"`; nhóm lớp thành `<fieldset>/<legend>` + ô tick có focus ring; bản chép **thứ 6** của `<select>` → `NativeSelect`; `<ul>/<li>`, `text-sm`, `EmptyState`, token `--success`/`--warning` thay `emerald`/`amber` thô; gom logic nhóm theo lớp chép 2 bản + 5 unit test. **`UX-UIUX-M00-018`** (user duyệt): luật 44px cho nút icon chưa bao giờ áp được vì Radix `asChild` **ghi đè `data-slot`** — đổi selector sang `[data-size^="icon"]`. Ngoài kế hoạch: sửa nốt **1 chỗ `D-12` sót từ đợt 4** ở `exam-attempt.tsx` (màn thi học viên). E2E M17 **12/12** + M16 chạy lại **16/16** + a11y **2/2** + M23 **2/2**; Vitest 220/220; lint/typecheck/build xanh. | ⏳ Chờ Codex/user xác minh độc lập — **Claude là người sửa, không tự ghi Verified** |
| UX-UIUX-M16-001…006 | M11           | (Claude fix, `P17-T1`) Bài tập giáo viên: **hạn nộp lệch múi giờ** — `toLocaleString("vi-VN")` lấy múi giờ MÁY giáo viên (Berlin 15:05 vs VN 20:05 cho cùng một hạn), đổi sang `formatDateTime` theo `D-12`; ngân hàng câu hỏi **tràn ngang 67px** @360 do `shrink-0` khoá cụm badge ở 398px; **11 control không có nhãn gọi được** (8 form giao bài + 3 bộ lọc) + nhóm lớp thành `<fieldset>/<legend>`; gom chuỗi class `<select>` chép 6 chỗ thành `NativeSelect` (viền 1.27:1 → **3.39:1**); danh sách thành `<ul>/<li>`, meta `text-xs` → `text-sm`, `EmptyState`. **Giữ nguyên `<input type="checkbox">` gốc cho `class_ids`** vì gửi nhiều giá trị cùng tên là hành vi nghiệp vụ. E2E **14/14** (Chromium + Pixel 7) gồm một bài giao bài THẬT rồi đọc DB xác nhận `attempt_limit`; Vitest 209/209; lint/typecheck/build xanh; fixture tự dọn, 5 bảng đếm cuối = 0. | ⏳ Chờ Codex/user xác minh độc lập — **Claude là người sửa, không tự ghi Verified** |
| UX-UIUX-M21-009 · UX-UIUX-M25-010 | M21, M17 | (Claude fix, `P15-T9`) Soát liên module M20–M27 ở **sáu** bề rộng 360/390/430/768/1024/1280. Hai lỗi thật mà spec từng module không bắt được: (1) dải 7 tab `/student/class` cuộn ngang ở 360px **không focus được** → bàn phím không tới được ba tab cuối (ba dải tab học viên còn lại đã đúng từ trước, chỉ màn này sót); (2) ô số liệu bento chép **ba bản** và đã trôi khác nhau ở `tabular-nums` → gom vào `StudentStatCard` dùng chung, lấy hợp của cả ba. Không đổi query/action/RPC/RLS/route/phân quyền/công thức/nhãn, không migration. E2E liên module **10/10** (Chromium + Pixel 7); unit mới **5/5**; full Vitest **209/209** (60 file); lint/typecheck/build xanh. | ⏳ Chờ Codex/user xác minh độc lập — **Claude là người sửa, không tự ghi Verified** |
| UX-UIUX-M23-001…009 | M12           | (Codex fix, `P15-T4`) Thi học viên: summary/card/status semantic, phòng chờ ba bước, lượt thi đủ context/timer/live status/heading, wrapper kết quả nhất quán; giữ fullscreen/integrity/anti-leak/player/timer/save/submit/scoring/query/RLS. Target 11/11; Playwright Chromium + Pixel 7 xanh cho list/waiting/attempt/result ở 360/768/1280 + axe A/AA + không overflow; full Vitest 180/180, lint/typecheck/build xanh. Fixture local tự dọn và năm count cuối bằng 0.                 | ⏳ Chờ Claude/user xác minh độc lập audio/micro/fullscreen, integrity và submit end-to-end |
| UX-UIUX-M22-001…009 | M11           | (Codex fix, `P15-T3`) Bài tập học viên: list summary/tab/card, lượt làm đủ context/live status/heading, kết quả semantic + progressbar; giữ scoring/submission/anti-leak/player/query/RLS. Target 6/6; Playwright Chromium + Pixel 7 xanh cho list/attempt/result ở 360/768/1280 + axe A/AA + không overflow; full Vitest 179/179, lint/typecheck/build xanh. Fixture local tự dọn và count cuối bằng 0.                                                                                  | ⏳ Chờ Claude/user xác minh độc lập dữ liệu thật, audio/micro và submit end-to-end         |
| UX-UIUX-M21-001…008 | M21           | (Codex fix, `P15-T2`) `/student/class` có class identity banner, 7 tab journey có icon, section `<h2>` thật, metadata ≥14px, mobile stack và semantic palette student-only. Giữ read-only/query/route/RLS/công thức; không sửa shared calendar/material. Component 2/2; Playwright Chromium + Pixel 7 xanh, 360/768/1280 + axe A/AA + không overflow; full Vitest 176/176, lint/typecheck/build xanh.                                                                                     | ⏳ Chờ Claude/user xác minh độc lập hình ảnh, dữ liệu thật và đủ 7 tab                     |
| UX-UIUX-M20-001…005 | M17           | (Codex fix, `P15-T1`) Dashboard học viên chuyển sang Learning Journey Bento: hero buổi kế tiếp, KPI amber/cyan/coral qua token WCAG, heading thật, metadata ≥14px và mobile stack. Component 2/2; Playwright Chromium 1/1 ở 360/768/1280 + axe A/AA + không overflow; full Vitest 175/175, lint/typecheck/build xanh. Không đổi query/route/RLS/nghiệp vụ.                                                                                                                                | ⏳ Chờ Claude/user xác minh độc lập hình ảnh và dữ liệu thật                               |
| UX-M22-M24-001      | M11, M12, M22 | (Codex fix, `P14-T12`) Player audio học viên dùng chung cho audio nguồn ở lượt làm/kết quả Bài tập/Thi, Flashcard và Ôn câu sai: mặc định `1×`, đúng ba tốc độ `0.5×/0.75×/1×`, giữ cao độ nếu hỗ trợ, reset khi URL đổi; bản ghi Nói của học viên không có tốc độ. Component test mục tiêu 8/8, full Vitest 173/173, lint/typecheck/build xanh.                                                                                                                                          | ⏳ Chờ Claude/user smoke media thật, keyboard/touch và xác minh độc lập                    |
| UX-M22-003          | M22           | (Claude fix) Trang mở đầu có nút xóa (lưu trữ) như mọi trang khác trong màn admin flashcard. Migration 69: `archive_flashcard_page` bỏ chặn `session_cover` và không dồn thứ tự khi bỏ cover (trang từ vựng giữ `order_index > 0`); `reorder_flashcard_pages` chỉ bắt cover đứng đầu khi buổi còn cover, đánh số từ 1 khi thiếu cover. Publish vẫn bị trigger chặn tới khi thêm lại trang mở đầu.                                                                                         | ⏳ Chờ Codex/user xác minh độc lập trên UI thật                                            |
| UX-M22-002          | M22           | (Claude fix) Form thêm trang theo loại: trang mở đầu chỉ nhập 2 ảnh, trang từ vựng chỉ nhập từ/cụm từ + audio + 2 ảnh; bỏ ô mô tả mặt trước/sau (alt do server sinh). Reader học viên: bỏ vùng tối + nhãn mặt/từ trên thẻ, desktop hiện trọn ảnh (`object-contain`, cao 560px, mobile giữ nguyên), rời trang nào thì trang đó reset về mặt trước, audio là một nút mang đúng tiêu đề trang thay cho thanh `<audio controls>`. Migration 68 cho `audio_path` nullable + check theo `kind`. | ⏳ Chờ Codex/user xác minh độc lập trên desktop/mobile với media thật                      |
| UX-M22-001          | M22           | Thay animation chỉ đưa trang mới vào bằng hai nửa của một cú lật toàn bộ flashcard quanh tâm: phải→trái khi tiến, chiều ngược khi lùi; lật mặt dưới↔trên dùng state riêng theo từng page, giữ nguyên khi sang/lùi trang; có reduced-motion và component test.                                                                                                                                                                                                                             | ⏳ Chờ Claude/user xác minh hướng chuyển động trên desktop/mobile với media thật           |
| UX-M11-M12-001      | M11, M12      | Đồng bộ builder Bài tập/Thi: đánh dấu câu đã chọn theo câu gốc, bắt buộc nhập điểm, preview modal cuộn, tổng điểm giao bài lấy từ bộ; kỳ thi tick nhiều lớp qua RPC transaction; thay toàn bộ native confirm bằng dialog theo theme.                                                                                                                                                                                                                                                      | ⏳ Chờ Claude xác minh UI độc lập                                                          |
| UX-M11-M12-002      | M11, M12      | Nộp bài có loading + kiểm lỗi lưu cuối + toast + chuyển tab Đã nộp; preflight micro theo câu Nói; chế độ tập trung khi thi ẩn dashboard/cảnh báo rời trang; ngân hàng câu hỏi dùng thao tác Chỉnh sửa nhưng vẫn giữ version lịch sử.                                                                                                                                                                                                                                                      | ⏳ Chờ Claude xác minh UI và thiết bị độc lập                                              |
| UX-M11-M12-003      | M11, M12      | Màn chấm bỏ JSON/enum/thuật ngữ kỹ thuật; câu trống là Chưa chấm, cảnh báo sót và một nút lưu bulk atomic; delivery gọn theo lớp; kết quả học viên hiển thị nội dung dễ đọc; sửa header `microphone=(self)` và chẩn đoán policy.                                                                                                                                                                                                                                                          | ⏳ Chờ Claude xác minh UI, bulk save và micro trên bản deploy                              |
| UX-M11-M12-004      | M11, M12      | Thêm nút quay lại đúng danh sách ở kết quả học viên và màn chấm giáo viên; phân trang kẹp trang vượt giới hạn, giữ filter, không bọc Link trong nút disabled.                                                                                                                                                                                                                                                                                                                             | ⏳ Chờ Claude xác minh UI và URL `page=3` độc lập                                          |
| UX-M11-M12-005      | M11, M12      | Nút bắt đầu Bài tập/Thi dùng trạng thái pending chung: hiện spinner + nhãn đang mở, khóa nút chống bấm lặp cho tới khi tạo attempt và redirect.                                                                                                                                                                                                                                                                                                                                           | ⏳ Chờ Claude/user smoke độ trễ thật sau redeploy                                          |
| BUG-M11-M12-003     | M11, M12, M18 | Query preview bộ lấy `prompt_content` + `question_media`, tạo signed URL private 5 phút và truyền vào cùng renderer học viên.                                                                                                                                                                                                                                                                                                                                                             | ⏳ Chờ Claude xác minh MP3 thật ở preview bộ bài tập/bộ đề                                 |
| BUG-M11-004         | M11, M18      | Dừng thu thì tự upload; chuẩn hóa MIME Chrome `audio/webm;codecs=opus`; chặn nộp khi đang thu/tải/lỗi và cho retry. RPC/answer_media giữ nguyên, pgTAP 291/291.                                                                                                                                                                                                                                                                                                                           | ⏳ Chờ Claude xác minh Chrome/Safari/mobile và màn chấm                                    |
| BUG-M11-M12-005     | M11, M12, M18 | MP3/M4A của Question Bank upload thẳng browser → Storage bằng vé server ký, server kiểm path/MIME/size thật trước khi gắn version; tạo mới và chỉnh sửa dùng chung luồng, mở lại editor phát được audio hiện tại.                                                                                                                                                                                                                                                                         | ⏳ Chờ Claude/user smoke MP3 thật sau redeploy                                             |
| BUG-M11-M12-006     | M11, M12, M18 | Bản ghi Nói của học viên không còn đi qua Server Action 1 MB: Bài tập/Thi dùng signed direct upload chung, server kiểm owner/attempt/item/path/MIME/size rồi RPC gắn đáp án; bổ sung `SELECT answer_media` qua RLS để màn chấm ký URL được.                                                                                                                                                                                                                                               | ⏳ Chờ Claude/user smoke micro thật và màn chấm sau redeploy                               |
| BUG-M11-M12-007     | M11, M12, M18 | RLS metadata + Storage cho phép học viên đọc MP3 đề chỉ khi câu thuộc đúng lượt Bài tập/Thi đang mở của mình; lượt khác/học viên khác fail-closed. Migration 63–64 đã áp cloud.                                                                                                                                                                                                                                                                                                           | ⏳ Chờ Claude/user smoke tài khoản học viên trên bản deploy                                |
| BUG-M11-M12-008     | M11, M12, M18 | Audit đủ ma trận audio: pipeline chung ký audio đề + bản ghi Nói cho Bài tập/Thi; màn giáo viên chấm và kết quả học viên đều render hai loại. RLS mở đúng delivery giáo viên và kết quả đã công bố của chính học viên; migration 65 đã áp cloud.                                                                                                                                                                                                                                          | ⏳ Chờ Claude/user smoke đủ 6 bề mặt audio trên bản redeploy                               |
| PERF-M20-001        | M01, M02, M20 | Xác minh JWT request thường bằng `getClaims()`/ES256 thay vì gọi Auth server lặp lại; helper fail-closed, role + `is_active` vẫn đọc từ `profiles`, RLS giữ nguyên; `getUser()` chỉ còn ở reset mật khẩu cần Auth record mới nhất.                                                                                                                                                                                                                                                        | ⏳ Chờ Claude/user đo production trước/sau deploy                                          |
| BR-M10-M13-001      | M10, M13      | Thêm công thức thuần `max(0, 10 - absent_count × 0,5)`; hiện điểm `/10` ở dashboard/lịch học viên và các màn tiến độ giáo viên, vẫn giữ tỉ lệ chuyên cần riêng cho điều kiện hoàn thành khóa.                                                                                                                                                                                                                                                                                             | ⏳ Chờ Claude xác minh nghiệp vụ độc lập                                                   |
| UX-M08-005          | M08           | Dùng chung `SessionCalendar` ba chế độ Tối giản/Tuần/Tháng cho admin, tab Lịch/Buổi giáo viên và Lịch học học viên; hành động/badge thay đổi đúng vai trò.                                                                                                                                                                                                                                                                                                                                | ⏳ Chờ Claude xác minh UI responsive độc lập                                               |

### Đã xác minh độc lập

| Bug ID      | Module   | Fix của Codex                                                                                                                                                   | Xác minh của Claude                                                                                                                                                                                                                                                                                                                                                                                                                     | Trạng thái      |
| ----------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| BUG-M08-001 | M08      | `getSessionLog` fail-fast khi route param không phải UUID → `notFound()` thay vì gửi chuỗi rác xuống Postgres và trả 500.                                       | **(2026-07-14, phiên 18) Góc soi riêng, không chạy lại test của Codex.** Rủi ro thật của một guard **không phải** "URL rác còn 500" mà là **guard chặn nhầm đường đi đúng** — nên kiểm đủ 3 ca: (a) 4 URL rác (`khong-phai-uuid`, `123`, `../etc`) → **404**, không 500; (b) UUID **hợp lệ nhưng của lớp GV B** → vẫn **404** (guard không nới lỏng IDOR); (c) **REGRESSION**: buổi học của chính GV A → **200** và render đúng LOP-02. | ✅ **Verified** |
| BUG-M11-002 | M11      | `getSubmissionGradingBoard` fail-fast khi route param không phải UUID → `notFound()` thay vì 500.                                                               | **(2026-07-14, phiên 18)** Cùng bộ 3 ca như trên: URL rác → **404**; assignment của chính GV A → **200**, hiện đúng bảng chấm bài. Đường đi đúng không bị guard chặn nhầm.                                                                                                                                                                                                                                                              | ✅ **Verified** |
| BUG-M06-001 | M06, M18 | Upload tài liệu ở cấp khóa học chấp nhận `module_id`/`lesson_id` không được gửi; mở server action tài liệu cho teacher rồi để RLS khoanh course được phân công. | _(2026-07-14, phiên 14)_                                                                                                                                                                                                                                                                                                                                                                                                                | ✅ **Verified** |
| BUG-M11-001 | M11      | Trigger khởi tạo submission chuẩn hóa mọi user flow có `auth.uid()` (kể cả super admin), không chỉ role student.                                                | _(2026-07-14, phiên 14)_                                                                                                                                                                                                                                                                                                                                                                                                                | ✅ **Verified** |

> Chi tiết bằng chứng của `BUG-M06-001` và `BUG-M11-001`: xem `WORKLOG.md` phiên 14.

---

## Điểm rủi ro cao — QA phải soi kỹ (rút từ bug có thật ở POLYMIND APP)

| #    | Rủi ro                                                                                       | Module        | Bug tương ứng ở hệ cũ                                             |
| ---- | -------------------------------------------------------------------------------------------- | ------------- | ----------------------------------------------------------------- |
| R-1  | **Race condition ghi danh vượt sĩ số** — kiểm ở app thay vì `FOR UPDATE` trong transaction   | M09           | — (thiết kế mới chặn sẵn)                                         |
| R-2  | **Idempotency chỉ ở app-level** → bulk attendance / sinh buổi / receipt bị trùng khi gọi lại | M08, M10, M14 | `BUG_M09_01` (trả hoa hồng 2 lần vì thiếu unique index)           |
| R-3  | **Attribution sai** — `created_by`/`marked_by` lấy "user đầu tiên" thay vì actor thật        | M04, M10, M14 | `BUG_M06_01`, `BUG_M12_01`                                        |
| R-4  | **Fail-open trong hàm phân quyền** — nhánh `return true` mặc định                            | M02           | `CR-M14-3` (`MessagingPolicy.CanMessage` → "nhắn loạn xạ")        |
| R-5  | **Nhiều đường ghi cho cùng một hành động** → lệch trạng thái                                 | M14           | `BUG_M10_01` (3 đường set `Payment→Paid`)                         |
| R-6  | **Export bỏ qua filter đang chọn**                                                           | M16           | `BUG_M16_01`                                                      |
| R-7  | **Dữ liệu chưa publish bị lộ** cho học viên                                                  | M11, M12      | — (thiết kế mới cưỡng chế bằng RLS)                               |
| R-8  | **IDOR qua object_path / signed URL**                                                        | M18           | `OBS-M18-01/02/03`                                                |
| R-9  | **Teacher đọc được tuition/audit**                                                           | M14, M19      | `CR-M16-1`, `CR-M17-1` (KPI tài chính lộ cho role không được xem) |
| R-10 | **Service role dùng cho user flow** → vô hiệu hóa toàn bộ RLS                                | M02, M03      | — (luật cứng ở `AGENTS.md`)                                       |

---

## Cấu trúc hồ sơ mỗi module

Khi bắt đầu QA một module, tạo `docs/testing/modules/<ID>-<tên>/`:

```text
01-analysis.md            Phân tích scope + luồng + điểm rủi ro
02-business-flows.md      Luồng nghiệp vụ chi tiết
03-test-cases.md          Test case (gồm negative path + IDOR)
04-traceability.md        Ánh xạ requirement ↔ test case
05-automation-report.md   Test tự động: lệnh chạy + kết quả THẬT (pass/fail)
06-bug-report.md          Bug tìm được → chuyển Codex
07-fix-report.md          Codex ghi: sửa gì, file nào, test gì
08-verification-report.md Claude xác minh ĐỘC LẬP → Verified / Failed
```
